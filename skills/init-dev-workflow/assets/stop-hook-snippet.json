{
  "hooks": {
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "bash -c 'CHANGED=$(git diff --name-only HEAD 2>/dev/null | grep -E \"{{CODE_PATHS_REGEX}}\" | head -1); if [ -n \"$CHANGED\" ]; then echo \"[dev-workflow] 检测到 {{CODE_PATHS_HUMAN}} 下有代码变更，请运行 /simplify 检查代码质量\"; fi'"
          }
        ]
      }
    ]
  }
}
